export const SETUP_SQL_SCRIPT = `-- This script has been run during the initial setup and is no longer needed.
-- This file is kept to prevent build errors from missing imports but can be safely removed
-- along with the /database-setup page if desired.
`;
