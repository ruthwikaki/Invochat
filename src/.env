# -----------------------------------------------------------------------------
# REQUIRED - Supabase Credentials
# Your application will not run without these. Find them in your Supabase
# project's "Project Settings > API" section.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# This is the full URL of your deployed application. It's crucial for Supabase
# authentication redirects (e.g., magic links, password resets).
# For local development, this is typically http://localhost:3000
# For production, it should be your actual domain, e.g., https://myapp.com
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# This is a secret key used for server-side operations that require admin
# privileges, like interacting with the Supabase Vault.
# Found in "Project Settings > API". Keep this key secure and never expose it
# to the client-side.
SUPABASE_SERVICE_ROLE_KEY=""


# -----------------------------------------------------------------------------
# REQUIRED - Google AI API Key
# Required for all generative AI features in the application.
# Get this from the Google AI Studio: https://aistudio.google.com/app/apikey
# -----------------------------------------------------------------------------
GOOGLE_API_KEY=""


# -----------------------------------------------------------------------------
# REQUIRED - Security & Health Checks
# -----------------------------------------------------------------------------
# These are used for securely encrypting integration API keys in the database.
# Generate these once and keep them safe.
# To generate a key (32 bytes / 64 hex chars): openssl rand -hex 32
ENCRYPTION_KEY=""
# To generate an IV (16 bytes / 32 hex chars): openssl rand -hex 16
ENCRYPTION_IV=""

# A secret key to protect the /api/health endpoint from public access.
# Generate a secure key using: openssl rand -hex 32
HEALTH_CHECK_API_KEY=""


# -----------------------------------------------------------------------------
# OPTIONAL - Caching & Integrations
# Enable these features by providing the required credentials.
# -----------------------------------------------------------------------------
# A Redis instance is recommended for production to enable caching and robust
# rate limiting. You can get a free instance from services like Upstash.
REDIS_URL=""

# Webhook secrets are used to verify that incoming webhook requests are
# genuinely from the specified platform, preventing spoofing attacks.
SHOPIFY_WEBHOOK_SECRET=""
WOOCOMMERCE_WEBHOOK_SECRET=""


# -----------------------------------------------------------------------------
# OPTIONAL - Email Service (Resend)
# Required for sending user invitations and data export notifications.
# -----------------------------------------------------------------------------
RESEND_API_KEY=""
# The email address that system emails will be sent from.
EMAIL_FROM=""
# For non-production environments, all emails will be sent to this address
# to prevent accidentally emailing real users.
EMAIL_TEST_RECIPIENT=""
