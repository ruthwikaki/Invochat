# ------------------------------------------------------------------------------
# InvoChat Environment Variables
#
# Make a copy of this file and name it .env
# Then, fill in the values below.
# ------------------------------------------------------------------------------

# --- Application URL ---
# The full URL of where your application is running. For local development,
# this will be http://localhost:3000. For production, it will be your domain.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Supabase ---
# You can find these values in your Supabase project's "Project Settings" > "API" section.
NEXT_PUBLIC_SUPABASE_URL="YOUR_SUPABASE_URL"
# This key is safe to be exposed in the browser.
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"
# IMPORTANT: This key is a secret and provides full access to your database.
# Keep it secure and never expose it on the client-side.
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

# --- Google AI ---
# Required for all AI features.
# 1. Go to Google AI Studio: https://aistudio.google.com/
# 2. Click "Get API key" and create a new key in a project.
# 3. Copy the key here.
GOOGLE_API_KEY="YOUR_GOOGLE_AI_API_KEY"

# --- Sentry (Error Monitoring) ---
# Optional, but recommended for production.
# Required for uploading source maps to Sentry for readable error stack traces.
# 1. Go to your Sentry account settings: https://sentry.io/settings/
# 2. Navigate to "Developer Settings" > "Auth Tokens".
# 3. Create a new token with "org:write" and "project:write" scopes.
SENTRY_AUTH_TOKEN="YOUR_SENTRY_AUTH_TOKEN"
