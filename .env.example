
# Supabase Configuration
# You can get these from your Supabase project settings.
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# Google AI (Genkit) Configuration
# You can get this from the Google AI Studio or Google Cloud Console.
GOOGLE_API_KEY="your-google-api-key"

# Application URL
# The public URL of your application.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
SITE_URL="http://localhost:3000"

# Redis (Optional but Recommended)
# If you have a Redis instance, provide the connection URL here for caching and rate limiting.
# Example: redis://:your-password@your-host:your-port
# REDIS_URL=""

# Encryption Keys (Required)
# These are critical for securing integration credentials.
# You MUST generate your own unique keys.
# Use the following commands in your terminal to generate them:
# openssl rand -hex 32 (for a 64-char key)
# openssl rand -hex 16 (for a 32-char IV)
ENCRYPTION_KEY=""
ENCRYPTION_IV=""

# Webhook Secrets (Optional)
# These are used to secure webhooks from integrated platforms.
# You create these secrets on the platform (e.g., Shopify) and add them here.
SHOPIFY_WEBHOOK_SECRET=""
WOOCOMMERCE_WEBHOOK_SECRET=""

# Health Check API Key (Required)
# A secret key to protect the /api/health endpoint. Generate a random string.
HEALTH_CHECK_API_KEY=""

# Email Service (Optional)
# If you want to send emails (e.g., for data exports), provide your Resend API key.
# RESEND_API_KEY=""
# EMAIL_FROM="noreply@yourdomain.com"
# EMAIL_TEST_RECIPIENT="your-test-email@example.com"
